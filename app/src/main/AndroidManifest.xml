<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- INTERNET: si vas a enviar datos a servidor (para compartir archivo no hace falta) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Ubicación -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- Actividad física (pasos, movimiento) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" /> <!-- Estado del teléfono (muy limitado lo que te deja leer hoy en día) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TelemetryPhone">
        <activity
            android:name=".TelemetryActivity"
            android:exported="false"
            android:label="@string/title_activity_telemetry"
            android:theme="@style/Theme.TelemetryPhone" />
        <activity
            android:name=".WebViewFormActivity"
            android:exported="false"
            android:label="@string/title_activity_webview_form"
            android:theme="@style/Theme.TelemetryPhone" />
        <activity
            android:name=".RecogidaDatosActivity"
            android:exported="false"
            android:label="@string/title_activity_recogida_datos"
            android:theme="@style/Theme.TelemetryPhone" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TelemetryPhone">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>